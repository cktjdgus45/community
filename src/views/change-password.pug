extends base.pug

block content 
    if(errorMessage)
            p=errorMessage 
    form(method="POST",autocomplete="off")
        input(name="oldPassword",type="password",placeholder="기존 비밀번호",required,autocomplete="new-password")
        input(name="newPassword",type="password",placeholder="새 비밀번호",required,autocomplete="new-password")
        input(name="newPasswordConfirmation",type="password",placeholder="새 비밀번호 재입력",required,autocomplete="new-password")
        input(type="submit" value=" 비밀번호 변경하기")