doctype html 
html(lang='ko')
    head 
        title #{pageTitle} | Catch Me
        link(rel="stylesheet" href="https://unpkg.com/mvp.css")
        script(src="https://kit.fontawesome.com/920f3667b2.js" crossorigin="anonymous")
    body
        header 
            nav 
                ul 
                    li 
                        a(href="/") 용인대 커뮤니티
                    if loggedIn                        
                        li 
                            a(href="/users/logout") 로그아웃                                               
                        li
                            a(href="/news/upload") 새 글 쓰기
                    else 
                        li 
                            a(href="/login") 로그인
                        li 
                            a(href="/join") 회원가입                                       
        main 
            block content
        include partials/footer.pug
        script(src="/assets/js/main.js")
        block scripts